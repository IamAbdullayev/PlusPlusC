#include <iostream>
#include <iomanip>
#include <cmath>
#include <cstring>
#include <math.h>

using namespace std;

bool isEven(int number) {
	if(number % 2 == 0) {
		return true;
	} else {
		return false;
	}
}